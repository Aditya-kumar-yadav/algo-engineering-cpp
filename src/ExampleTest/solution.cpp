#include <iostream>
using namespace std;

int main() {
    int a, b;
    // Reads two integers from input
    if (cin >> a >> b) {
        // Outputs their sum
        cout << a + b << endl;
    }
    return 0;
}